# Local dev: Vite proxies /api → backend (see vite.config.js)
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:3000

# Production (Vercel): defaults to https://api.dater.social — no env needed.
# Override only if using a different API host:
# VITE_API_BASE_URL=https://api.dater.social
